#ifndef LOGGING_HPP
#define LOGGING_HPP

#include <iostream>
#include <thread>

class Logging
{
    private:
        
    public:
        /* constructors */
        Logging();               
        Logging(const Logging& other);
        
        /* operator overload */
        Logging& operator=(const Logging& other);
        
        /* destructor */
        ~Logging();

        /* member functions */

        /* getter */

        /* setter */
};

#endif
